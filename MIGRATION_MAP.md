# Migration map

Each row maps a migrated block to the old code it came from. Files are moved with `git mv`
(history is kept; `git log --follow` works). The old module path re-exports the new crate,
so callers are unchanged until the layer that owns them is migrated.

| New location | Old location | Change | Tag |
|---|---|---|---|
| crates/core/package_id/src/lib.rs | src/host_engine/core/package_id.rs (whole file) | moved as-is; behavior tests added before the move; old path now `pub use tg_core_package_id as package_id` | refactor/core-package_id-done |
| crates/core/fault/src/lib.rs | src/host_engine/core/fault.rs (whole file) | moved; `CapturedPanic`, `is_supervised`, `current_fault_domain`, `capture_panic` widened from pub(crate) to pub (used by core/crash); old path re-exports | refactor/core-fault-done |
| crates/core/geometry/src/lib.rs | src/host_engine/services/layout/types.rs (whole file) | moved as-is; Rect::contains behavior test added before the move (pins saturating right/bottom edge); layout re-exports via `use tg_core_geometry as types` | refactor/core-geometry-done |
| crates/core/version/src/lib.rs | src/host_engine/services/version.rs (whole file) | moved as-is; constants pinned by a test before the move; CARGO_PKG_VERSION now read from workspace.package (still 2.0.0) | refactor/core-version-done |
| crates/core/atomic_fs/src/lib.rs | src/host_engine/services/storage/atomic.rs (whole file) | moved; atomic_write/atomic_replace_with widened pub(crate)->pub; existing 2 tests served as pre-move behavior tests; storage re-exports via `use tg_core_atomic_fs as atomic` | refactor/core-atomic_fs-done |
| crates/core/arena/src/lib.rs | src/host_engine/services/animation/pool.rs lines 11-71 (`Arena`, `ArenaSlot`) | block moved verbatim, pub(crate)->pub, module doc added; behavior test added before the move; pool.rs now `pub(crate) use tg_core_arena::Arena`; audio/pool.rs imports the core directly (removes audio->animation dependency). Exception: only 2 users (animation, audio) — extracted because it is a zero-dependency generic container and removes a service->service edge | refactor/core-arena-done |
| crates/core/style/src/{color,style,types}.rs | src/host_engine/services/rich_text/{color,style,types}.rs (whole files) | closed group moved as-is (color/types only depend on style); behavior tests for style tags and RGB reversal added before the move; rich_text/mod.rs re-exports from tg_core_style, callers unchanged | refactor/core-style-done |
| crates/core/input/src/{events,key_token,action_map}.rs, key.rs | src/host_engine/services/input/{events,key_token,action_map}.rs (whole files); key.rs = service.rs lines 27-211 (`Key`..`InputActionEvent`, `KeyPattern` impl) | files moved as-is (only `super::service` -> `super::key` imports changed); type block cut verbatim; KeyPattern behavior test added before the move and moved with the block; InputService keeps crossterm/rdev capture and imports the core; input/mod.rs re-exports, callers unchanged | refactor/core-input-done |
| crates/core/fault/src/crash.rs | src/host_engine/core/crash.rs (whole file) | merged into the fault core (crash depends on fault, so per PLAN they merge); dependency on TerminalService inverted: `install_panic_hook(restore_terminal: fn())`, the main loop passes `TerminalService::force_restore`; phase round-trip test added before the move; fault smoke now installs the hook and checks a supervised panic is captured without restoring the terminal; core/mod.rs re-exports from tg_core_fault | refactor/core-crash-done |
| crates/core/log/src/{level,phase,source,message,entry}.rs | src/host_engine/services/log/{level,phase,source,message,entry}.rs (whole files) | closed group moved as-is; HostLogMessage::render widened pub(crate)->pub (LogService renders templates); source/phase/level behavior test added before the move; formatter/labels (labels needs I18nService) and LogService stay in the service; log/mod.rs re-exports, callers unchanged | refactor/core-log-done |
| crates/core/unicode/src/{measure,types}.rs | src/host_engine/services/unicode/{measure,types}.rs (whole files) | first removed dead code (rich_text_width, line_display_width, TextDirection, BidiRun — the only link to RichText) in a separate commit, then moved as-is; existing 13 tests served as pre-move behavior tests; unicode-width/unicode-segmentation moved to workspace.dependencies; UnicodeService stays in services (its methods are still unused — handle in service phase); unicode/mod.rs re-exports | refactor/core-unicode-done |
| crates/core/style/src/{cell,frame}.rs | src/host_engine/services/canvas/cell.rs, src/host_engine/services/render_pipeline/frame.rs (whole files) | merged into the style core (CanvasCell depends on TextStyle, ComposedFrame on CanvasCell; 6 users each); behavior tests added before the move; CanvasBuffer (2 users) stays in canvas; canvas/render_pipeline mod.rs re-export, callers unchanged | refactor/core-style-cell-done |
| crates/core/audio/src/lib.rs | src/host_engine/services/audio/types.rs (ids, AudioState, AudioErrorCode/AudioError, ResolvedAudioFile, AudioSource, AudioAsyncEvent) | file git-moved to the core; AudioType/AudioPlaybackSnapshot/AudioObject cut back into a new services/audio/types.rs; pub(crate) ctors/accessors widened to pub (storage/package construct them); identity/error-format test added before the move; audio/mod.rs re-exports. Unblocks storage->audio edge. Note: pub items in a lib crate are not dead-code linted (warnings 233->230) | refactor/core-audio-done |
| crates/core/sandbox_path/src/lib.rs | src/host_engine/services/lua/path.rs (whole file) | pure std, used by lua/async_runtime/export/image/package/recording/screenshot/video/ui -> core; pub(crate)->pub; existing 2 tests are the behavior tests; lua/mod.rs `pub(crate) use tg_core_sandbox_path as path` keeps `lua::path::*` | refactor/core-sandbox_path-done |

## Services

| New location | Old location | Change | Tag |
|---|---|---|---|
| crates/service/terminal/src/{lib,capabilities}.rs | src/host_engine/services/terminal.rs, terminal_capabilities.rs (whole files) | terminal_capabilities (1 user) merged into the terminal crate; moved as-is; existing capability test is the behavior test; smoke never enters raw mode; crossterm moved to workspace.dependencies; workspace members now include crates/service/*; services/mod.rs re-exports TerminalService | refactor/service-terminal-done |
| crates/service/clipboard/src/lib.rs | src/host_engine/services/clipboard.rs (whole file) | removed dead cfg(test) in-memory scaffolding (`memory` ctor/field, never used), kept `unavailable()` for a new behavior test added before the move; arboard dependency moved from root into the crate; smoke restores the previous clipboard text | refactor/service-clipboard-done |
| crates/service/ffmpeg/src/lib.rs | src/host_engine/services/ffmpeg.rs (whole file) | moved as-is (std only); existing 4 tests are the behavior tests; smoke probes the real installation | refactor/service-ffmpeg-done |
| crates/service/input_method/src/lib.rs | src/host_engine/services/input_method.rs (whole file) | moved as-is; im-switch dependency moved from root into the crate; existing 9 tests (fake backend) are the behavior tests; smoke only detects, never switches the IME | refactor/service-input_method-done |
| crates/service/popup/src/lib.rs | src/host_engine/services/popup.rs (whole file) | moved as-is; depends on style core; existing 3 tests are the behavior tests | refactor/service-popup-done |
| crates/service/host_object/src/lib.rs | src/host_engine/services/host_object.rs (whole file) | moved; depends on geometry core; ensure_area/update_area/clear widened pub(crate)->pub (used by runtime/host_viewport); existing test is the behavior test | refactor/service-host_object-done |
| crates/service/code_highlight/src/lib.rs | src/host_engine/services/code_highlight/mod.rs (whole file) | moved as-is; depends on style core; all tree-sitter grammar deps moved from root into the crate; include_str! asset paths keep the same depth (still repo-root assets/); existing 5 tests are the behavior tests. NOTE: `cargo run -p` alone recompiles grammar C sources and fails on this machine (msys2 cc1.exe exits 1 — local toolchain issue); verified via `cargo build --workspace --examples` + running the example binary | refactor/service-code_highlight-done |
| crates/service/rich_text/src/{lib,params,parser,service}.rs | src/host_engine/services/rich_text/{mod,params,parser,service}.rs (whole files) | moved after the TextMode cycle cut; depends on style + input cores; parse_mode widened pub(crate)->pub (text_layout); dead `parser::parse` wrapper removed (tests call parse_auto); existing 13 tests are the behavior tests; services/mod.rs keeps `use tg_service_rich_text as rich_text` so callers are unchanged | refactor/service-rich_text-done |
| crates/service/text_layout/src/lib.rs | src/host_engine/services/text_layout.rs (whole file) | moved; depends on rich_text service + unicode core; pub(crate) items used by canvas/layout/widget/ui widened to pub (to_text_style stays crate-private); unicode-linebreak moved from root into the crate; existing 15 tests are the behavior tests; services/mod.rs `pub use tg_service_text_layout as text_layout` keeps paths | refactor/service-text_layout-done |
| crates/service/log/src/{lib,formatter,labels,service}.rs | src/host_engine/services/log/{mod,formatter,labels,service}.rs (whole files) | moved after the log->i18n/file cycle cut; depends on log + atomic_fs + package_id cores and chrono (now a workspace dependency); existing 8 tests are the behavior tests; services/mod.rs `use tg_service_log as log` keeps paths | refactor/service-log-done |
| crates/service/storage/src/{lib,bootstrap,game_save,layout,profile,service}.rs | src/host_engine/services/storage/* (whole files) | moved after: audio data model core (storage->audio edge gone) and atomic_fs callers importing the core directly; depends on log service + atomic_fs/audio/package_id cores + serde; UI-owned inherent impls on Display* enums split: `next`/`target_fps` moved into storage (value semantics, new test), i18n key lookups became free fns in ui/home/settings/display_settings.rs; `from_root_for_test` gated by `test-support` feature (root enables it under [dev-dependencies]); smoke builds a real layout in a temp root | refactor/service-storage-done |
| crates/service/i18n/src/*, crates/service/i18n/build.rs | src/host_engine/services/i18n/* (whole files), build.rs (root) | moved; depends on log + storage services; root build.rs (only generated the embedded en_us table) git-moved into the crate with the assets path made relative to the crate (`../../../assets/...`); root [build-dependencies] removed; i18n build-deps keep the exact old spec so the build-script feature set is unchanged (otherwise tree-sitter C code recompiles — broken local gcc); embedded-fallback test added before the move | refactor/service-i18n-done |
| crates/service/random/src/{lib,objects,service}.rs | src/host_engine/services/random.rs + widget/runtime_object/random/mod.rs | cycle cut first (generator objects moved from widget into random; RandomService takes &mut RandomGeneratorObjects; 26 call sites pass `pool.random_generators`); RandomGeneratorObjects::new/len/is_empty pub (+Default); rand/rand_chacha moved from root into the crate; existing 8 tests are the behavior tests | refactor/service-random-done |
| crates/service/animation/src/* | src/host_engine/services/animation/* (whole files) | moved after the RuntimeObjectPool inversion (animation owns AnimationObjects); depends on arena + style cores; AnimationId::new widened (test id construction), AnimationObjects::animation_count added for logo tests; existing 23 tests are the behavior tests; dead code recorded in temp/handoff/DEAD_CODE.md before the move (lib crates hide unused pub items: warnings 221->195) | refactor/service-animation-done |
| crates/service/layout/src/{lib,measure,position,service}.rs | src/host_engine/services/layout/* (whole files) | moved after deleting the dead pool-based resolvers (layout->widget edge); depends on geometry core + rich_text/text_layout services + crossterm (was an undeclared implicit dependency: terminal size in measure.rs); only externally used pub(crate) methods widened (resize_physical, set_developer_viewport, resolve_host_x); existing tests are the behavior tests | refactor/service-layout-done |
| crates/service/canvas/src/{lib,buffer,service,surface,top_layer}.rs | src/host_engine/services/canvas/* (whole files) | moved after the canvas<-widget inversion (surface ids/scrollbar types in surface.rs; prepare takes Vec<SurfaceFrame>; UiObjectPool::prepare_canvas builds them); 4 widget-driven tests moved to widget surface module, 4 rewritten to build SurfaceFrames or fill via text_at; depends on geometry/style/unicode cores + layout/rich_text/text_layout services + crossterm; buffer module, Prepared* types and 19 externally used methods widened to pub | refactor/service-canvas-done |
| crates/service/async/src/{lib,write_barrier}.rs | src/host_engine/services/async_runtime.rs (TaskId, ManagedThreadId, TaskCancellation, TaskState, WorkerMessage, AsyncRuntime, worker_loop, cancel/state helpers) + services/write_barrier.rs | executor extracted and made generic over the event type (`AsyncRuntime<E>`, jobs implement `AsyncJob<E>`: run / write_target / cancelled_before_start / reports_own_cancellation; executor status via `E: From<TaskStatusEvent>`); main keeps `type AsyncRuntime = tg_service_async::AsyncRuntime<EngineEvent>` and `impl AsyncJob<EngineEvent> for EngineTask`, so submit call sites are unchanged; shutdown/stop_* moved to an unbounded impl; TaskCancellation::new/cancel behind `test-support` (root dev-dependency) | refactor/service-async-done |
| crates/service/time/src/{lib,objects,service}.rs | src/host_engine/services/time.rs + widget/runtime_object/time/mod.rs | moved after two cuts: timer objects moved out of widget (TimeObjects, RuntimeObjectPool.time) and the sleep task became time-owned (SleepTask: AsyncJob<E>, TimeAsyncEvent; EngineTask::Sleep removed, EngineEvent: From<TimeAsyncEvent>); depends only on the async executor crate; existing tests are the behavior tests; dead code recorded in DEAD_CODE.md | refactor/service-time-done |
| crates/service/file/src/{lib,task}.rs | src/host_engine/services/file.rs + async_runtime.rs (FileTask, FileEvent, FileListEntry, run_file_task, Lua file/i18n helpers and their tests) | moved after the file task became file-owned (FileTask: AsyncJob<E>, EngineTask::File removed, EngineEvent: From<FileEvent>); write-barrier temporary path reuses atomic_fs `temporary_path` (made pub) instead of a copy; depends on atomic_fs/sandbox_path cores + async executor + chardetng/encoding_rs/serde_json (chardetng dropped from the root); existing 13 tests are the behavior tests; dead FileService wrapper recorded in DEAD_CODE.md | refactor/service-file-done |
| crates/service/image/src/lib.rs | src/host_engine/services/image.rs (whole file) + async_runtime.rs (ImageTask, ImageEvent, run_image_task) | moved after the image task became image-owned (ImageTask: AsyncJob<E>, EngineTask::Image removed, EngineEvent: From<ImageEvent>); cache format version read from the version core; 2 async behavior tests added before the move (async output == sync output then Finished; invalid params -> ImageEvent::Failed + TaskStatusEvent::Failed); depends on version core + async executor + image(png, jpeg)/serde/serde_json; root keeps `image` for screenshot; dead async path recorded in DEAD_CODE.md | refactor/service-image-done |
| crates/service/network/src/{lib,executor,security}.rs | src/host_engine/services/network/* (whole files) | moved after the network task became network-owned (NetworkTask: AsyncJob<E> with cancelled_before_start/reports_own_cancellation; EngineTask::Network removed); `NetworkError::at` widened (used by the Lua broker); reqwest moved from the root; existing 20 tests are the behavior tests | refactor/service-network-done |
