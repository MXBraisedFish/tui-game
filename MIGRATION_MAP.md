# Migration map

Each row maps a migrated block to the old code it came from. Files are moved with `git mv`
(history is kept; `git log --follow` works). The old module path re-exports the new crate,
so callers are unchanged until the layer that owns them is migrated.

| New location | Old location | Change | Tag |
|---|---|---|---|
| crates/core/package_id/src/lib.rs | src/host_engine/core/package_id.rs (whole file) | moved as-is; behavior tests added before the move; old path now `pub use tg_core_package_id as package_id` | refactor/core-package_id-done |
| crates/core/fault/src/lib.rs | src/host_engine/core/fault.rs (whole file) | moved; `CapturedPanic`, `is_supervised`, `current_fault_domain`, `capture_panic` widened from pub(crate) to pub (used by core/crash); old path re-exports | refactor/core-fault-done |
| crates/core/geometry/src/lib.rs | src/host_engine/services/layout/types.rs (whole file) | moved as-is; Rect::contains behavior test added before the move (pins saturating right/bottom edge); layout re-exports via `use tg_core_geometry as types` | refactor/core-geometry-done |
| crates/core/version/src/lib.rs | src/host_engine/services/version.rs (whole file) | moved as-is; constants pinned by a test before the move; CARGO_PKG_VERSION now read from workspace.package (still 2.0.0) | refactor/core-version-done |
| crates/core/atomic_fs/src/lib.rs | src/host_engine/services/storage/atomic.rs (whole file) | moved; atomic_write/atomic_replace_with widened pub(crate)->pub; existing 2 tests served as pre-move behavior tests; storage re-exports via `use tg_core_atomic_fs as atomic` | refactor/core-atomic_fs-done |
| crates/core/arena/src/lib.rs | src/host_engine/services/animation/pool.rs lines 11-71 (`Arena`, `ArenaSlot`) | block moved verbatim, pub(crate)->pub, module doc added; behavior test added before the move; pool.rs now `pub(crate) use tg_core_arena::Arena`; audio/pool.rs imports the core directly (removes audio->animation dependency). Exception: only 2 users (animation, audio) — extracted because it is a zero-dependency generic container and removes a service->service edge | refactor/core-arena-done |
| crates/core/style/src/{color,style,types}.rs | src/host_engine/services/rich_text/{color,style,types}.rs (whole files) | closed group moved as-is (color/types only depend on style); behavior tests for style tags and RGB reversal added before the move; rich_text/mod.rs re-exports from tg_core_style, callers unchanged | refactor/core-style-done |
| crates/core/input/src/{events,key_token,action_map}.rs, key.rs | src/host_engine/services/input/{events,key_token,action_map}.rs (whole files); key.rs = service.rs lines 27-211 (`Key`..`InputActionEvent`, `KeyPattern` impl) | files moved as-is (only `super::service` -> `super::key` imports changed); type block cut verbatim; KeyPattern behavior test added before the move and moved with the block; InputService keeps crossterm/rdev capture and imports the core; input/mod.rs re-exports, callers unchanged | refactor/core-input-done |
| crates/core/fault/src/crash.rs | src/host_engine/core/crash.rs (whole file) | merged into the fault core (crash depends on fault, so per PLAN they merge); dependency on TerminalService inverted: `install_panic_hook(restore_terminal: fn())`, the main loop passes `TerminalService::force_restore`; phase round-trip test added before the move; fault smoke now installs the hook and checks a supervised panic is captured without restoring the terminal; core/mod.rs re-exports from tg_core_fault | refactor/core-crash-done |
| crates/core/log/src/{level,phase,source,message,entry}.rs | src/host_engine/services/log/{level,phase,source,message,entry}.rs (whole files) | closed group moved as-is; HostLogMessage::render widened pub(crate)->pub (LogService renders templates); source/phase/level behavior test added before the move; formatter/labels (labels needs I18nService) and LogService stay in the service; log/mod.rs re-exports, callers unchanged | refactor/core-log-done |
| crates/core/unicode/src/{measure,types}.rs | src/host_engine/services/unicode/{measure,types}.rs (whole files) | first removed dead code (rich_text_width, line_display_width, TextDirection, BidiRun — the only link to RichText) in a separate commit, then moved as-is; existing 13 tests served as pre-move behavior tests; unicode-width/unicode-segmentation moved to workspace.dependencies; UnicodeService stays in services (its methods are still unused — handle in service phase); unicode/mod.rs re-exports | refactor/core-unicode-done |
| crates/core/style/src/{cell,frame}.rs | src/host_engine/services/canvas/cell.rs, src/host_engine/services/render_pipeline/frame.rs (whole files) | merged into the style core (CanvasCell depends on TextStyle, ComposedFrame on CanvasCell; 6 users each); behavior tests added before the move; CanvasBuffer (2 users) stays in canvas; canvas/render_pipeline mod.rs re-export, callers unchanged | refactor/core-style-cell-done |

## Services

| New location | Old location | Change | Tag |
|---|---|---|---|
| crates/service/terminal/src/{lib,capabilities}.rs | src/host_engine/services/terminal.rs, terminal_capabilities.rs (whole files) | terminal_capabilities (1 user) merged into the terminal crate; moved as-is; existing capability test is the behavior test; smoke never enters raw mode; crossterm moved to workspace.dependencies; workspace members now include crates/service/*; services/mod.rs re-exports TerminalService | refactor/service-terminal-done |
| crates/service/clipboard/src/lib.rs | src/host_engine/services/clipboard.rs (whole file) | removed dead cfg(test) in-memory scaffolding (`memory` ctor/field, never used), kept `unavailable()` for a new behavior test added before the move; arboard dependency moved from root into the crate; smoke restores the previous clipboard text | refactor/service-clipboard-done |
| crates/service/ffmpeg/src/lib.rs | src/host_engine/services/ffmpeg.rs (whole file) | moved as-is (std only); existing 4 tests are the behavior tests; smoke probes the real installation | refactor/service-ffmpeg-done |
| crates/service/input_method/src/lib.rs | src/host_engine/services/input_method.rs (whole file) | moved as-is; im-switch dependency moved from root into the crate; existing 9 tests (fake backend) are the behavior tests; smoke only detects, never switches the IME | refactor/service-input_method-done |
| crates/service/popup/src/lib.rs | src/host_engine/services/popup.rs (whole file) | moved as-is; depends on style core; existing 3 tests are the behavior tests | refactor/service-popup-done |
